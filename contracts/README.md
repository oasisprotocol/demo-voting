NOTE: this is an ESM compatible package for the typechain generated APIs

This is necessary because hardhat + esm = massive pain, so we symlink the src

This is used by `-frontend` instead of the `-hardhat` package
